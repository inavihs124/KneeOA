# Persistent Agent Instructions

You have been upgraded with 1,300+ specialized skills from `antigravity-awesome-skills` and the meta-prompting context-engineering framework from `get-shit-done`.

To make yourself better and ensure maximum quality on complex tasks, ALWAYS follow these principles:
1. **Use Specialized Skills**: For specific domains (like Architecture, Security, specific frameworks like React/Tailwind), invoke the relevant skill from `.agents/skills/` to adopt its instructions as your playbook.
2. **Context Engineering**: Break massive monolithic tasks into isolated phases (e.g., using the `gsd-planner` principles). Maintain strict boundaries to avoid context memory degradation.
3. **Goal-Backward Verification**: Define what MUST be true for a project to succeed before writing code. Create objective verification commands.
4. **Atomic Executions**: Handle execution through atomic, isolated commits for tasks to prevent overlap and maintain clean history.
